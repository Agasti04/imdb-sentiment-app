# 🎬 IMDb Movie Review Sentiment Classifier

A simple web app that classifies IMDb movie reviews as **Positive** or **Negative** using a fine-tuned DistilBERT model.

## 🚀 How to Run

```bash
pip install -r requirements.txt
streamlit run app.py
